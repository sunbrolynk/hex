# Supply-chain hardening for the frontend (see docs/SUPPLY_CHAIN.md).
# Exact versions on any future `npm install` (we pin, never float).
save-exact=true
# Refuse to install under an unsupported Node/npm (engines in package.json).
engine-strict=true
# Quiet, deterministic installs.
fund=false
audit=true
